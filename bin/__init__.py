from os.path import dirname
from os.path import realpath
from os.path import join

massscan_path = join(dirname(realpath(__file__)), 'masscan')
hydra_path = join(dirname(realpath(__file__)), 'hydra')
